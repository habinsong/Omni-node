# Omni-node v1.0.2 업데이트 내역

작성일: 2026-05-13

이번 업데이트는 Omni-node를 단순 대화/코딩 실행 도구에서, 대화·코딩·텔레그램·루틴·로직·노트북·작업계획이 서로 이어지는 작업 환경으로 끌어올리는 데 초점을 맞췄다. 특히 사용자가 실제로 입력한 요청이 더 잘 유지되고, 생성된 결과가 실행 가능하며, 결과를 다시 계획·노트·루틴으로 재사용할 수 있게 만드는 쪽을 보강했다.

## 핵심 요약

- 코딩탭의 생성 기준을 “코드가 나왔다”에서 “요구사항을 구현했고 실행·검증할 수 있다”로 강화했다.
- Python 게임과 외부 패키지 처리 문제를 고쳤고, Python 외에도 Node/TypeScript/React/C#/Java/C/C++/Go/Rust 등 여러 언어와 프로젝트 구조를 더 잘 다루도록 확장했다.
- 대화탭과 텔레그램에서 이어지는 질문의 맥락을 더 안정적으로 유지하도록 보강했다.
- 스킬 모드와 Think+ 모드를 함께 사용할 수 있게 하고, 텔레그램에서도 스킬/Think+ 흐름이 더 일관되게 동작하도록 정리했다.
- NVIDIA NIM 제공자를 추가해 대화, 코딩, 오케스트레이션, 다중 LLM, 텔레그램에서 사용할 수 있게 했다.
- 루틴탭, 로직탭, 노트북탭, 작업계획탭을 대화/코딩/텔레그램 흐름과 연결해 실제로 재사용할 수 있게 했다.
- 외부접속과 모바일 반응형 UI를 보강했다.
- 회귀 방지를 위한 계약 테스트를 추가했다.

## 코딩탭 개선

### 언어 자동 선택과 프로젝트 구조

- `auto` 언어 선택이 무조건 Python으로 떨어지는 문제를 보완했다.
- 요청 내용을 보고 언어뿐 아니라 프로젝트 형태를 함께 판단하도록 구조를 확장했다.
- 단일 파일만 만들려는 흐름을 줄이고, 일반 코딩 요청은 여러 파일이 필요한 프로젝트 구조를 우선하도록 개선했다.
- Python, Node, TypeScript, React/Vite, C#, Java, C/C++, Go, Rust, Bash/CLI 등 다양한 작업 유형을 더 잘 구분하도록 했다.

### 실행 가능성 검증 강화

- Python 외부 패키지는 run workspace 내부 `.venv`를 우선 사용해 설치하도록 보강했다.
- `pygame` 같은 게임 라이브러리 자동 설치 차단을 제거했다.
- 게임 요청은 테스트용 로그만 찍는 더미 코드가 아니라 실제 게임 루프, 이벤트 처리, 렌더링, 상태 처리를 포함하도록 검증을 강화했다.
- headless 환경에서도 게임 smoke 검증이 가능하도록 `OMNI_HEADLESS_TEST=1`, `SDL_VIDEODRIVER=dummy` 흐름을 추가했다.
- React/Vite/HTML/Canvas 계열은 브라우저 smoke 검증을 추가할 수 있도록 기반을 보강했다.
- Windows/macOS/Linux별 실행 명령을 분리해 POSIX 전용 명령이 Windows 검증 명령에 섞이지 않도록 했다.

### 요구사항 충족 게이트

- 사용자 프롬프트에서 요구사항을 체크리스트로 뽑고, 실행이 성공해도 핵심 요구사항이 빠지면 성공으로 보지 않도록 품질 게이트를 강화했다.
- TODO, placeholder, 빈 함수, print-only 게임, 껍데기 UI 같은 결과를 실패로 판단하도록 했다.
- 실패 원인에 따라 복구 프롬프트가 달라지도록 개선했다.
- 다중 LLM 코딩 결과는 실행, 요구사항 충족, 파일 구성, 테스트, 런타임 정보를 기준으로 더 나은 결과를 고르도록 보강했다.

## 대화탭과 텔레그램 개선

### 이어지는 질문의 맥락 보강

- “그럼 왜?”, “예시는?”, “다시 설명해줘”처럼 이전 질문에 이어지는 요청을 더 잘 감지하도록 했다.
- 텔레그램에서도 직전 사용자 질문과 직전 assistant 답변을 함께 참고하도록 개선했다.
- 이어지는 질문인데 모델 응답이 새 요청과 어긋났을 때 무리하게 맥락을 끊지 않도록 drift recovery 조건을 보수적으로 조정했다.
- 완전히 다른 질문은 이전 맥락에 억지로 묶지 않도록 후속 질문 감지 기준을 분리했다.

### 스킬과 Think+

- 스킬 모드와 Think+ 모드를 동시에 사용할 수 있게 했다.
- 스킬을 한 번 사용하면 사용자가 명시적으로 중지하기 전까지 이어지도록 sticky skill 흐름을 보강했다.
- 텔레그램에서도 스킬 사용, 중지, Think+ 조합이 대화탭과 더 비슷하게 동작하도록 정리했다.
- 스킬 생성 시 `SKILL.md`가 너무 짧고 품질이 낮게 생성되는 문제를 보완했다.

### 텔레그램 출력과 첨부 처리

- 텔레그램 긴 응답이 단순 줄 수 기준으로 잘리는 문제를 줄였다.
- 코드블록, 표, 긴 설명이 더 안전하게 전달되도록 응답 formatting을 개선했다.
- 텔레그램 자연어 명령 처리에서 노트북 저장, 계획 생성, 코딩 결과 재사용 흐름을 추가했다.

## NVIDIA NIM 제공자 추가

- 새 LLM 제공자로 `NVIDIA NIM`을 추가했다.
- canonical provider key는 `nvidia`다.
- `nvidia-nim`, `nvidia_nim`, `nim` alias도 `nvidia`로 정규화한다.
- 기본 base URL은 `https://integrate.api.nvidia.com/v1`이다.
- 기본 모델은 `meta/llama-3.1-70b-instruct`다.
- 대화탭, 코딩탭, 오케스트레이션, 다중 LLM, 텔레그램 라우팅에서 사용할 수 있게 했다.
- OpenAI 호환 chat completions, streaming, 202 polling 흐름을 지원하도록 구현했다.
- NVIDIA API 키 설정 UI와 provider health/runtime 표시를 추가했다.

## 외부접속과 모바일 UI

- 대시보드 외부접속을 설정탭에서 켜고 끌 수 있게 했다.
- 외부접속 시 상태 문구가 `세션 인증됨`이 아니라 `외부 접속중`으로 표시되도록 했다.
- 외부접속 클라이언트에서는 OTP, Telegram, LLM 키, CLI 인증 같은 민감 설정 UI와 서버 액션을 차단했다.
- 외부접속 주소를 설정탭에서 확인할 수 있게 했다.
- 모바일 대화탭/코딩탭에서 composer가 화면 하단과 겹치지 않도록 개선했다.
- 스마트폰 세로 화면에서는 키보드 버튼으로 composer를 열고, 아래 화살표로 최소화할 수 있게 했다.
- 모바일 설정탭의 탭 UI와 하단 버튼 배치를 정리했다.

## 루틴탭 개선

- 대화/코딩 입력에서 바로 루틴 생성폼으로 보낼 수 있게 했다.
- 루틴 생성 시 기본값을 “저장만 하고 즉시 실행하지 않음”으로 정리했다.
- 즉시 실행 여부를 사용자가 명확하게 선택할 수 있게 했다.
- 루틴 preview, scheduler status, retry, notify 설정 흐름을 보강했다.
- 텔레그램과 루틴 명령 연결을 개선했다.
- 루틴 계약 테스트를 추가했다.

## 로직탭 개선

- 저장되지 않은 최신 그래프 초안도 실행 요청에 반영될 수 있도록 개선했다.
- 로직 실행 전 최신 상태 저장/검증 흐름을 보강했다.
- 실행 결과 조회와 상태 메시지 처리를 개선했다.
- 로직탭 계약 테스트를 추가했다.

## 노트북탭 개선

- 대화 답변을 노트북에 바로 저장할 수 있게 했다.
- 최근 코딩 결과를 노트북 검증 기록으로 바로 저장할 수 있게 했다.
- 텔레그램에서도 최근 답변과 최근 코딩 결과를 노트북에 저장할 수 있게 했다.
- 노트북 문서 전체 보기, 검색, 메타데이터 저장을 보강했다.
- 대화/코딩 입력 준비 시 관련 노트북 컨텍스트를 주입할 수 있게 했다.
- 노트북 계약 테스트를 추가했다.

## 작업계획탭 개선

- 대화/코딩 입력창에서 현재 입력을 바로 작업계획 생성폼으로 보낼 수 있게 했다.
- assistant 답변에서 바로 작업계획을 만들 수 있게 했다.
- 최근 코딩 결과에서 이어서 할 작업계획을 만들 수 있게 했다.
- 텔레그램에서 “최근 답변으로 계획”, “최근 코딩 결과로 계획” 같은 자연어 shortcut을 추가했다.
- 계획 생성 LLM 출력은 JSON 구조를 우선 사용하도록 바꿨다.
- JSON 파싱 실패 시 기존 줄 목록 파서 fallback을 유지해 안정성을 확보했다.
- 작업계획 계약 테스트를 추가했다.

## 회귀 방지 테스트

이번 업데이트에서 추가하거나 확장한 계약 테스트:

- `scripts/check-coding-python-game-contract.mjs`
- `scripts/check-chat-telegram-contract.mjs`
- `scripts/check-logic-tab-contract.mjs`
- `scripts/check-routine-tab-contract.mjs`
- `scripts/check-notebook-tab-contract.mjs`
- `scripts/check-plan-tab-contract.mjs`

검증한 명령:

```bash
dotnet build apps/omninode-middleware/OmniNode.Middleware.csproj
npm test
```

두 검증 모두 통과했다.

## 변경된 주요 영역

- 대시보드 UI: `apps/omninode-dashboard/`
- 미들웨어 명령 처리: `apps/omninode-middleware/src/CommandService.*.cs`
- 코딩 실행/검증: `CommandService.Coding*`
- 텔레그램 처리: `CommandService.Telegram*`, `TelegramUpdateLoop.cs`
- 루틴/로직/노트북/계획 서비스: `Application/*`, `Ws*Dispatcher.cs`
- 테스트 스크립트: `scripts/check-*.mjs`, `scripts/run-omninode-tests.mjs`

## 비전공자용 설명

이번 업데이트를 쉽게 말하면, Omni-node가 “말만 해주는 AI”에서 “작업을 이어서 관리하는 작업 도구”에 더 가까워졌다.

예전에는 사용자가 “파이썬으로 테트리스 만들어줘”라고 하면 코드가 나오긴 해도, 실제로 필요한 라이브러리가 설치되지 않거나, 게임 창이 뜨지 않거나, 테스트용 문장만 출력하고 끝나는 문제가 생길 수 있었다. 이제는 이런 경우를 더 엄격하게 검사한다. 게임이면 실제 게임처럼 움직이는 코드인지, 필요한 패키지를 설치했는지, 실행할 수 있는지, 요구한 기능이 빠지지 않았는지 확인한다.

대화도 개선됐다. 사용자가 “그럼 왜?”, “예시 들어줘”, “다시 설명해줘”처럼 앞 질문에 이어서 말하면, 이제 이전 질문과 이전 답변을 더 잘 참고한다. 반대로 완전히 다른 질문을 하면 이전 주제에 억지로 묶지 않도록 했다.

텔레그램 봇도 대화탭과 더 비슷하게 동작하도록 고쳤다. 스킬을 켜고 대화하거나, Think+를 같이 쓰거나, 최근 답변을 노트북에 저장하거나, 최근 코딩 결과를 작업계획으로 바꾸는 일이 가능해졌다.

노트북탭은 작업 중 배운 내용이나 결정사항, 검증 결과를 모아두는 곳이다. 이제 대화 답변이나 코딩 결과를 버튼 한 번으로 노트북에 저장할 수 있다. 나중에 다시 작업할 때 이전에 무슨 결정을 했는지 찾기 쉬워진다.

작업계획탭은 “이 일을 어떤 순서로 해야 하는지” 나눠주는 곳이다. 이제 대화 중 나온 답변이나 코딩 결과를 바로 작업계획으로 바꿀 수 있다. 예를 들어 코딩 결과가 실패했으면, 그 결과를 보고 “다음에 무엇을 고쳐야 하는지” 단계별 계획을 만들 수 있다.

루틴탭은 반복 작업을 저장해두는 기능이다. 이제 대화나 코딩 입력을 루틴으로 바로 넘길 수 있어서, 자주 반복하는 점검이나 실행 작업을 더 쉽게 저장할 수 있다.

로직탭은 여러 작업을 순서대로 이어 붙이는 흐름도 같은 기능이다. 이번 업데이트에서는 저장하지 않은 최신 흐름도 실행에 반영되도록 개선했다.

외부접속과 모바일 화면도 개선했다. 다른 기기에서 대시보드를 볼 수 있고, 휴대폰 화면에서는 입력창이 메시지를 가리지 않도록 열고 닫는 방식이 개선됐다. 외부접속에서는 중요한 인증 정보나 API 키 설정이 보이지 않도록 막았다.

마지막으로 NVIDIA NIM이라는 새 AI 제공자를 추가했다. 이제 Groq, Gemini, Cerebras, Copilot, Codex뿐 아니라 NVIDIA NIM 모델도 대화와 코딩 작업에 사용할 수 있다.

정리하면 v1.0.2는 “더 많은 기능 추가”도 있지만, 핵심은 결과를 실제로 쓸 수 있게 만들고, 작업 흐름이 끊기지 않게 하는 업데이트다.
