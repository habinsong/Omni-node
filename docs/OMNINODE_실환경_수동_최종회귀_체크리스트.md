# Omni-node 실환경 수동 최종회귀 체크리스트

업데이트 기준: 2026-05-07

이 문서는 현재 코드 기준 기능을 사람이 직접 최종 확인할 때 쓰는 체크리스트다.
아래 `코멘트:`에는 실제 테스트 환경, 사용한 provider/model, 실패 원인만 간단히 적는다.
과거 판정 메모는 현재 기준이 아니므로 남기지 않는다.

## A. 서버/기본 상태

- [ ] A1. `http://127.0.0.1:8080/healthz` 응답 200
  코멘트:

- [ ] A2. `http://127.0.0.1:8080/readyz` 응답 200
  절차: 대시보드 접속 또는 WebSocket `ping/pong` 1회 이후 확인
  코멘트:

- [ ] A3. 대시보드 접속 정상 (`http://127.0.0.1:8080/`)
  코멘트:

## B. 설정/키 저장

- [ ] B1. 설정탭에서 Gemini API Key 저장 성공
  코멘트:

- [ ] B2. 설정탭에서 Cerebras API Key 저장 성공
  코멘트:

- [ ] B3. 서버 재시작 후 설정 상태 유지
  코멘트:

- [ ] B4. Copilot / Codex 상태 조회 정상
  코멘트:

- [ ] B5. 설정탭 `프로젝트 문맥` 패널에서 문맥 스캔 / skills / commands 새로고침 정상
  코멘트:

## C. 대화탭

- [ ] C1. 질문 1회 전송 시 답변 1회만 수신
  코멘트:

- [ ] C2. 메모리 연속성 확인
  절차: 별명 또는 이름을 알려준 뒤 다음 질문에서 다시 물어본다.
  코멘트:

- [ ] C3. 오케스트레이션 답변이 역할 분담 형태로 정리되어 보임
  예: 초안, 리스크 점검, 반례·예외, 실행안 구체화, 최종 누락 점검
  코멘트:

- [ ] C4. 다중 대화가 모델 캐러셀 + `공통 요약 / 공통 핵심 / 부분 차이` 구조로 표시됨
  코멘트:

- [ ] C5. URL 포함 질문에서 URL 컨텍스트 경로가 동작함
  코멘트:

## D. 코딩탭

- [ ] D1. 단일 코딩 1회 정상 완료
  코멘트:

- [ ] D2. 오케스트레이션 코딩이 `기획 -> 개발 -> 검증 및 테스트 -> 수정` 단계로 완주
  코멘트:

- [ ] D3. 다중 코딩이 provider/model별 하위 폴더를 만들고 비교 요약을 남김
  코멘트:

- [ ] D4. `최근 코딩 결과`가 새로고침 후 같은 대화에서 복원됨
  코멘트:

- [ ] D5. 다중 코딩 UI가 모델 캐러셀 + `공통 요약 / 공통점 / 차이 / 추천`으로 표시됨
  코멘트:

- [ ] D6. Safe Refactor 도크가 요구사항 입력 위에서 최소화 상태로 보이고, 중앙 오버레이로 열림
  코멘트:

- [ ] D7. `최근 코딩 결과`에서 파일 프리뷰와 `실행` 결과가 실제 엔트리 기준으로 표시됨
  코멘트:

- [ ] D8. 입력형 CLI 프로그램은 `stdin 입력` 후 재실행 가능
  코멘트:

## E. 로직탭

- [ ] E1. 로직 탭 진입 시 데스크톱 레이아웃이 `좌측 1/4 그래프 목록 + 중앙 2/4 캔버스 + 우측 1/4 노드 팔레트 + 하단 인스펙터`로 보임
  코멘트:

- [ ] E2. 새 그래프 기본 상태가 `시작` 노드 1개, `100%` 배율이며 노드 내용이 카드 밖으로 깨지지 않음
  코멘트:

- [ ] E3. 노드 카드 전체 드래그로 이동 가능하고, 배경 드래그로 팬, 휠 줌 시 페이지 스크롤이 같이 움직이지 않음
  코멘트:

- [ ] E4. 출력 포트 점에서 입력 포트 점으로 드래그할 때 프리뷰 선이 따라오고, 놓으면 연결 생성 및 선택 연결 삭제가 정상
  코멘트:

- [ ] E5. 그래프 목록에서 이름 수정, 저장, 실행이 가능하고 JSON import-export round-trip이 정상
  코멘트:

- [ ] E6. 하단 인스펙터에서 `직접 입력 / 목록에서 고르기 / 연결해서 받기`와 경로 `찾아보기`가 실제로 동작함
  코멘트:

- [ ] E7. 로직 실행 후 `logic_graph_run_event`가 실시간 갱신되고 `workspace/.runtime/logic/<graph-id>/<run-id>/graph.json`, `snapshot.json`, `events.log`가 남음
  코멘트:

## F. 루틴탭

- [ ] F1. 루틴 생성/수정/즉시 실행 정상
  코멘트:

- [ ] F2. 루틴 상세에서 `텔레그램 봇 응답 켜기/끄기`를 바꾸면 다음 예약 실행부터 자동 전송 여부가 실제로 바뀜
  코멘트:

- [ ] F3. 루틴 실행 이력에서 `telegram=not_applicable`가 웹 수동 실행에만 표시됨
  코멘트:

- [ ] F4. 예약 실행 또는 텔레그램 테스트 실행에서 실제 telegram 전송 결과가 남음
  코멘트:

- [ ] F5. `NotifyTelegram=false`인 루틴도 `텔레그램 테스트`와 실행 이력 `재전송`은 정상 동작함
  코멘트:

- [ ] F6. `/routine runs`, `/routine detail`, `/routine resend`에 대응하는 정보가 웹에서도 일치함
  코멘트:

## G. 노트북 / 자동화 / 설정 고급 도구

- [ ] G1. 노트북 탭에서 작업 메모, 방향 정리, 확인한 내용 저장과 이어보기 생성 정상
  코멘트:

- [ ] G2. 노트북 탭 `문서 보기`에서 작업 메모/방향 정리/확인한 내용/다음에 이어볼 것 카드가 16:9 화면에서는 2열로 정렬되고, 좁은 화면에서는 1열로 내려감
  코멘트:

- [ ] G3. 자동화/계획 탭 내부 좌측 선택에서 `계획`과 `태스크 그래프` 전환 정상
  코멘트:

- [ ] G4. 자동화/계획 탭 `계획` 화면에서 계획 생성 -> 리뷰 -> 승인 -> 실행 정상
  코멘트:

- [ ] G5. 자동화/계획 탭 `계획용 LLM`에서 planner/reviewer provider, model, fallback chain이 화면 밖으로 나가지 않음
  코멘트:

- [ ] G6. 자동화/계획 탭 `태스크 그래프` 화면에서 graph 생성, 실행, task output 조회 정상
  코멘트:

- [ ] G7. 자동화/계획 탭 `그래프 실행 라우팅`에서 주요 라우팅과 보조 라우팅이 반응형으로 깨지지 않음
  코멘트:

- [ ] G8. 설정탭 `환경 진단`에서 `core_socket` 포함 doctor 항목 정상
  코멘트:

- [ ] G9. 설정탭 `고급 도구 > 도구 통합`에서 상태 요약, Provider 상태, Guard 관측, 제어 요청, 결과 확인 흐름이 정상
  코멘트:

## H. 텔레그램 봇

- [ ] H1. 일반 질문 1회 전송 시 답변 1회만 수신
  코멘트:

- [ ] H2. 자연어로 LLM 모드/제공자/모델 전환 가능
  예: `단일 모드로 바꿔`, `Codex로 바꿔`, `모델 목록 보여줘`
  코멘트:

- [ ] H3. 자연어로 코딩 실행/최근 결과/파일 프리뷰 가능
  예: `단일 코딩으로 로그인 페이지 만들어줘`, `최근 코딩 결과 보여줘`, `코딩 파일 1 보여줘`
  코멘트:

- [ ] H4. 자연어 또는 slash로 코딩 설정 변경 가능
  예: `단일 코딩 제공자를 Codex로 바꿔`, `/coding multi worker gemini gemini-2.5-pro`
  코멘트:

- [ ] H5. Safe Refactor 읽기/미리보기/적용 가능
  예: `refactor read ...`, `refactor preview ... ::: 새 코드`, `/refactor apply`
  코멘트:

- [ ] H6. 루틴 목록/수정/실행 이력/상세/재전송 가능
  예: `루틴 수정 rt-... ...`, `루틴 실행 이력 rt-...`, `/routine resend ...`
  코멘트:

- [ ] H7. `3.1`, `gemini-2.5-pro` 같은 소수점/버전 표기가 줄바꿈으로 깨지지 않음
  코멘트:

## I. 제공자 실요청

- [ ] I1. Gemini 실요청 1회 정상
  코멘트:

- [ ] I2. Groq 실요청 1회 정상
  코멘트:

- [ ] I3. Copilot 실요청 1회 정상
  코멘트:

- [ ] I4. Cerebras 실요청 1회 정상
  코멘트:

- [ ] I5. Codex 실요청 1회 정상
  코멘트:

## J. 최종 판정

- [ ] J1. 치명 장애 없음
  코멘트:

- [ ] J2. 문서와 실제 동작이 크게 어긋나지 않음
  코멘트:

- [ ] J3. 남은 이슈는 provider 제한이나 외부 API 상태 같은 운영 이슈로 분류 가능
  코멘트:
