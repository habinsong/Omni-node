# Omni-node 환경변수 및 상태 파일

업데이트 기준: 2026-05-07

이 문서는 경로, 상태, 시크릿을 다시 확인할 때 보는 기준 문서입니다. 전체 환경변수 목록과 기본값 정의는 `apps/omninode-middleware/src/AppConfig.cs`가 가장 정확합니다.

## 1. 기준 경로와 기억할 원칙

기본으로 기억할 canonical 경로는 아래와 같습니다.

| 구분 | 기준 경로 | 메모 |
|---|---|---|
| 앱 소스 | `apps/` | 실제 런타임 소스 |
| 문서 | `docs/` | canonical 문서 위치 |
| 작업공간 | `/path/to/Omni-node/workspace/coding` | `OMNINODE_WORKSPACE_ROOT` 기본 예시 |

원칙:

- 문서와 쉘 export 예시에서는 canonical 경로만 기본값으로 표기합니다.
- 루트 `coding`, `runtime`, `omninode-*`, 루트 문서 파일은 하위 호환 alias로만 봅니다.
- 실제 운영에서 `OMNINODE_WORKSPACE_ROOT`를 명시할 때도 canonical 경로를 우선 사용합니다.

## 2. 시크릿 로딩 우선순위

LLM/텔레그램 시크릿은 아래 순서로 로드됩니다.

1. macOS Keychain
2. 로컬 secure store(macOS 외 OS의 기본 보안 저장소)
3. `*_FILE`
4. 직접 환경변수

중요 사항:

- 직접 환경변수는 deprecated 경고를 남깁니다.
- `*_FILE`은 권한이 `0600` 또는 그보다 엄격해야 합니다.
- 문서의 `*_FILE` 예시는 `~/.omninode/keys/` 디렉터리를 기준으로 듭니다.
- 로컬 secure store 기본 경로는 macOS/Linux `~/.config/omninode/secrets.json`, Windows `%APPDATA%\omninode\secrets.json`입니다.

## 3. 자주 쓰는 환경변수

### 3.1 서버/경로

| 환경변수 | 기본값 | 의미 |
|---|---|---|
| `OMNINODE_WS_PORT` | `8080` | HTTP/WebSocket 포트 |
| `OMNINODE_CORE_SOCKET_PATH` | macOS/Linux: `/tmp/omninode_core.<uid>.sock`, Windows: `tcp://127.0.0.1:51808` | 코어 IPC 엔드포인트 |
| `OMNINODE_DASHBOARD_INDEX` | 자동 탐색 | 대시보드 `index.html` 경로 |
| `OMNINODE_WORKSPACE_ROOT` | 자동 탐색 | 코딩 작업공간 루트 |
| `OMNINODE_SANDBOX_EXECUTOR` | `../omninode-sandbox/executor.py` | Python 샌드박스 실행기 경로 |
| `OMNINODE_GATEWAY_STARTUP_PROBE` | `false` | 게이트웨이 부트 프로브 활성화 |
| `OMNINODE_ENABLE_LOCAL_OTP_FALLBACK` | `true` | 텔레그램 실패 시 로컬 OTP fallback 사용 |
| `OMNINODE_DOCTOR_TIMEOUT_SECONDS` | `15` | doctor 개별 check 타임아웃 |
| `OMNINODE_DOCTOR_ENABLE_SANDBOX_SMOKE` | `true` | doctor에서 샌드박스 smoke 실행 여부 |
| `OMNINODE_DOCTOR_WRITE_HISTORY` | `true` | doctor history JSON 누적 저장 여부 |
| `OMNINODE_REFACTOR_ENABLE_LSP` | `false` | Safe Refactor의 LSP rename preview/apply 활성화 |
| `OMNINODE_REFACTOR_ENABLE_AST_GREP` | `false` | Safe Refactor의 ast-grep replace preview/apply 활성화 |
| `OMNINODE_REFACTOR_PREVIEW_TTL_MINUTES` | `120` | refactor preview JSON 보관 시간 |
| `OMNINODE_PROJECT_CONTEXT_FALLBACK_FILENAMES` | `TEAM_GUIDE.md,.agents.md` | project context에 함께 읽을 fallback 파일 목록 |
| `OMNINODE_PROJECT_CONTEXT_MAX_BYTES` | `65536` | 병합 instruction text 최대 바이트 |

### 3.2 제공자/모델

| 환경변수 | 기본값 | 의미 |
|---|---|---|
| `OMNINODE_GROQ_MODEL` | `meta-llama/llama-4-scout-17b-16e-instruct` | 기본 Groq 모델 |
| `OMNINODE_GEMINI_MODEL` | `gemini-3-flash-preview` | 일반 Gemini 모델 |
| `OMNINODE_GEMINI_SEARCH_MODEL` | `gemini-3.1-flash-lite-preview` | 검색 전용 Gemini 모델 |
| `OMNINODE_CEREBRAS_MODEL` | `gpt-oss-120b` | 기본 Cerebras 모델 |
| `OMNINODE_COPILOT_MODEL` | `gpt-5-mini` | 기본 Copilot 모델 |
| `OMNINODE_CODEX_MODEL` | `gpt-5.4` | 기본 Codex 모델 |
| `OMNINODE_COPILOT_BIN` | `gh` | GitHub CLI 경로 |
| `OMNINODE_COPILOT_DIRECT_BIN` | `copilot` | Copilot 직접 바이너리 |
| `OMNINODE_CODEX_BIN` | `codex` | Codex CLI 바이너리 |

운영 메모:

- 코딩 경로에서 Copilot은 현재 `gpt-5-mini`로 정규화해 사용합니다.
- 코딩 provider smoke 스크립트도 같은 기본 모델 집합을 기준으로 돕니다.

### 3.3 시크릿 입력

| 항목 | 직접 환경변수 | 파일 환경변수 |
|---|---|---|
| Telegram Bot Token | `OMNINODE_TELEGRAM_BOT_TOKEN` | `OMNINODE_TELEGRAM_BOT_TOKEN_FILE` |
| Telegram Chat ID | `OMNINODE_TELEGRAM_CHAT_ID` | `OMNINODE_TELEGRAM_CHAT_ID_FILE` |
| Groq API Key | `OMNINODE_GROQ_API_KEY` | `OMNINODE_GROQ_API_KEY_FILE` |
| Gemini API Key | `OMNINODE_GEMINI_API_KEY` | `OMNINODE_GEMINI_API_KEY_FILE` |
| Cerebras API Key | `OMNINODE_CEREBRAS_API_KEY` | `OMNINODE_CEREBRAS_API_KEY_FILE` |
| Codex API Key | `OMNINODE_CODEX_API_KEY` | `OMNINODE_CODEX_API_KEY_FILE` |

추가 제한:

- `OMNINODE_TELEGRAM_ALLOWED_USER_ID`: 허용할 텔레그램 사용자 제한

### 3.4 실행 한도/타임아웃

| 환경변수 | 기본값 | 의미 |
|---|---|---|
| `OMNINODE_CHAT_MAX_OUTPUT_TOKENS` | `8192` | 대화 최대 출력 토큰 |
| `OMNINODE_CODING_MAX_OUTPUT_TOKENS` | `16384` | 코딩 최대 출력 토큰 |
| `OMNINODE_LLM_TIMEOUT_SEC` | `20` | 일반 LLM 요청 타임아웃 |
| `OMNINODE_CODE_EXEC_TIMEOUT_SEC` | `120` | 코드 실행 타임아웃 |
| `OMNINODE_GEMINI_WEB_TIMEOUT_MS` | `30000` | Gemini grounding 웹 타임아웃 |
| `OMNINODE_WS_COMMANDS_PER_MINUTE` | `30` | WebSocket 분당 명령 제한 |
| `OMNINODE_WS_MAX_MESSAGE_BYTES` | `157286400` | WebSocket 최대 메시지 바이트 |

## 4. 기본 상태 파일 위치

상태 루트는 기본적으로 `~/.omninode`입니다. 홈 디렉터리를 얻지 못하면 `Path.GetTempPath()/omninode` 계열의 OS별 임시 경로로 fallback 합니다.

상태는 아래 세 층으로 기억하면 덜 헷갈립니다.

| 층 | 대표 경로 | 의미 |
|---|---|---|
| 영속 상태 | `~/.omninode` | 설정, 세션, 대화, 루틴 정의 원본 |
| 작업 산출물 | `workspace/` | 루틴 결과, 회귀 아티팩트, 작업 대상 파일 |
| 임시 실행 상태 | macOS/Linux `/tmp`, Windows 로컬 TCP, `workspace/.runtime`, `workspace/runtime` | 소켓, 락, 포트, 임시 스냅샷 |

### 4.1 `~/.omninode` 아래 주요 파일

| 경로 | 의미 |
|---|---|
| `llm_usage.json` | LLM 사용량 상태 |
| `copilot_usage.json` | Copilot 사용량 상태 |
| `conversations.json` | 대화 저장소(메시지, linked memory, 최근 코딩 결과 snapshot 및 다중 코딩 비교 메타 포함) |
| `auth_sessions.json` | 인증 세션 저장소 |
| `guard_retry_timeline.json` | guard retry timeline 상태 |
| `gateway_health.json` | gateway health 상태 |
| `gateway_startup_probe.json` | startup probe 상태 |
| `telegram_update_offset.txt` | 텔레그램 polling next update checkpoint |
| `telegram_update_loop.lock` | 텔레그램 polling 단일 인스턴스 락 파일 |
| `telegram_reply_outbox.json` | 텔레그램 응답 전송 실패 시 재전송 대기 큐 |
| `routines.json` | 루틴 정의 원본. 일반 루틴과 `logic_graph` 그래프를 함께 저장 |
| `routing-policy.json` | category별 provider override 정책 |
| `plans/index.json` | 계획 인덱스 |
| `plans/<plan-id>/plan.json` | 계획 본문 |
| `plans/<plan-id>/review.json` | 리뷰 결과 |
| `plans/<plan-id>/execution.json` | 최근 실행 결과 |
| `tasks/index.json` | task graph 인덱스 |
| `tasks/<graph-id>.json` | task graph 스냅샷 |
| `notebooks/<project-key>/learnings.md` | 프로젝트 learnings 기록 |
| `notebooks/<project-key>/decisions.md` | 프로젝트 decisions 기록 |
| `notebooks/<project-key>/verification.md` | 프로젝트 verification 기록 |
| `notebooks/<project-key>/handoff.md` | 최신 handoff 문서 |
| `doctor/last-report.json` | 최근 doctor 보고서 |
| `doctor/history/` | doctor 이력 JSON |
| `cli/middleware.pid` | `Omni-node` 전역 실행기가 관리하는 미들웨어 PID |
| `cli/core.pids` | `Omni-node` 실행 중 새로 부트스트랩한 코어 PID 목록 |
| `cli/middleware.log` | `Omni-node` 백그라운드 실행 로그 |
| `cli/last-start.env` | 최근 `Omni-node` 시작 메타데이터 |
| `AGENTS.md` | 전역 instruction 파일 |
| `skills/` | 전역 skill manifest 루트 |
| `commands/` | 전역 command template 루트 |
| `memory-notes/` | 메모리 노트 디렉터리 |
| `code-runs/` | 코드 실행 결과 메타데이터/로그 이력 |
| `memory-index/main.sqlite` | 메모리 검색 인덱스 DB |

### 4.2 `workspace/` 아래 주요 경로

| 경로 | 의미 |
|---|---|
| `workspace/coding/` | 앱이 다루는 작업 대상 워크스페이스 |
| `workspace/coding/runs/` | 코딩 탭 세 모드가 공유하는 실행별 작업 폴더 |
| `workspace/coding/routines/` | 루틴 실행 결과 이력 |
| `workspace/.runtime/` | 회귀 결과/임시 분석물 |
| `workspace/.runtime/logic/` | 로직 그래프 실행 스냅샷과 이벤트 로그 |
| `workspace/.runtime/coding-*.json` | provider/model/language smoke 결과 리포트 |
| `workspace/.runtime/tasks/` | task graph 실행 로그와 result.json |
| `workspace/.runtime/refactor-preview/` | Safe Refactor preview JSON |
| `workspace/runtime/` | 현재 세션 상태 스냅샷 |
| `workspace/coding/.omni/skills/` | 프로젝트 전용 skill manifest |
| `workspace/coding/.omni/commands/` | 프로젝트 전용 command template |

루틴은 특히 아래처럼 구분해서 봅니다.

- 루틴 정의를 보존하려면 `~/.omninode/routines.json`이 중요합니다.
- 로직 탭 그래프도 같은 `~/.omninode/routines.json`에 `ExecutionMode=logic_graph`와 `LogicGraph` 본문으로 저장됩니다.
- provider chain override를 유지하려면 `~/.omninode/routing-policy.json`을 같이 봐야 합니다.
- 코딩 탭 생성 파일까지 보존하려면 `workspace/coding/runs/`를 같이 봐야 합니다.
- 다중 코딩의 모델별 실제 산출물까지 보존하려면 `workspace/coding/runs/<실행별 폴더>/<provider>-<model>/` 하위 폴더까지 같이 봐야 합니다.
- provider/model/language 회귀 결과까지 보존하려면 `workspace/.runtime/coding-*.json`을 같이 봐야 합니다.
- 최근 코딩 결과 복원 상태까지 보존하려면 `~/.omninode/conversations.json`을 같이 봐야 합니다.
- 텔레그램 코딩의 최근 결과/파일 프리뷰도 같은 `conversations.json` snapshot과 `workspace/coding/runs/` 산출물을 같이 사용합니다.
- 최근 코딩 결과 `실행`은 검증 커맨드가 아니라 엔트리 실행 명령을 우선 구성하며, GUI/인터랙티브 앱은 별도 실행 유지 경로를 사용할 수 있습니다.
- 과거 실행 산출물까지 보존하려면 `workspace/coding/routines/`도 같이 봐야 합니다.
- 로직 그래프 실행 흔적까지 보존하려면 `workspace/.runtime/logic/<graph-id>/<run-id>/graph.json`, `snapshot.json`, `events.log`를 같이 봐야 합니다.
- Task graph 상태를 복구하려면 `~/.omninode/tasks/`와 `workspace/.runtime/tasks/`를 같이 봐야 합니다.
- notebook / handoff를 이어가려면 `~/.omninode/notebooks/`를 같이 봐야 합니다.
- Safe Refactor preview를 추적하려면 `workspace/.runtime/refactor-preview/`를 봅니다.
- 텔레그램 Safe Refactor의 `/refactor preview`도 같은 `workspace/.runtime/refactor-preview/` JSON을 사용합니다.
- 텔레그램 응답이 일시 실패하면 `~/.omninode/telegram_reply_outbox.json`에 잠시 적재되고, 다음 polling 루프에서 자동 재전송을 시도합니다.
- project context를 추적하려면 루트 `AGENTS.md`, 필요한 경우 `AGENTS.override.md`, 그리고 `.omni/skills/`, `.omni/commands/`를 같이 봅니다.
- 루틴 이력에서 `telegramStatus=not_applicable`는 웹 수동 실행이라 텔레그램 전송 대상이 아니었다는 의미입니다.
- 루틴 정의 원본 `~/.omninode/routines.json`에는 `NotifyTelegram` 값도 함께 저장되며, 이 값이 `false`면 예약 실행 결과를 텔레그램으로 자동 전송하지 않습니다.
- 일반 루틴 탭 목록은 `logic_graph` 항목을 보여주지 않으므로, 로직 탭 저장본을 찾을 때는 루틴 UI보다 상태 파일과 로직 전용 API 흐름을 기준으로 보는 편이 정확합니다.
- 자동화/계획 탭의 계획용 LLM과 graph 실행 라우팅 설정은 `~/.omninode/routing-policy.json` override 영향을 받습니다.
- 노트북 탭의 append/handoff 생성은 현재 LLM 상태 파일을 쓰지 않고 `~/.omninode/notebooks/` markdown 원본만 갱신합니다.

### 4.3 OS별 임시 IPC/로그 경로

macOS/Linux 코어 IPC는 `/tmp`를 사용합니다. Windows 코어 IPC는 파일 소켓 대신 로컬 TCP 루프백을 사용합니다. 감사 로그와 사용량/상태 파일의 기본값은 `~/.omninode` 아래로 모읍니다.

| 경로 또는 엔드포인트 | OS | 의미 |
|---|---|---|
| `/tmp/omninode_core.<uid>.sock` | macOS/Linux | 코어 UDS 소켓 |
| `/tmp/omninode.<uid>.lock` | macOS/Linux | 코어 단일 인스턴스 락 |
| `tcp://127.0.0.1:51808` | Windows | 코어 로컬 TCP 엔드포인트 |
| `~/.omninode/audit.log` | 공통 | 감사 로그 기본 위치 |

## 5. 운영에 자주 필요한 조합

### 5.1 최소 웹 대시보드 실행

```bash
export OMNINODE_WORKSPACE_ROOT="/path/to/Omni-node/workspace/coding"
export OMNINODE_ENABLE_LOCAL_OTP_FALLBACK=1
```

### 5.2 검색 기능 포함 실행

```bash
export OMNINODE_GEMINI_API_KEY_FILE="$HOME/.omninode/keys/gemini_api_key"
export OMNINODE_GEMINI_SEARCH_MODEL="gemini-3.1-flash-lite-preview"
```

### 5.3 텔레그램 포함 실행

```bash
export OMNINODE_TELEGRAM_BOT_TOKEN_FILE="$HOME/.omninode/keys/telegram_bot_token"
export OMNINODE_TELEGRAM_CHAT_ID_FILE="$HOME/.omninode/keys/telegram_chat_id"
export OMNINODE_TELEGRAM_ALLOWED_USER_ID="123456789"
```

## 6. 알아두면 좋은 구현 포인트

- 대시보드 경로는 실행 경로 기준으로 자동 탐색합니다.
- 워크스페이스 루트도 자동 탐색하지만, 실제 운영에서는 `OMNINODE_WORKSPACE_ROOT="/path/to/Omni-node/workspace/coding"`처럼 canonical 경로를 명시하는 편이 안전합니다.
- Codex는 API Key가 없더라도 CLI 로그인 상태가 살아 있으면 사용 가능할 수 있습니다.
- 시크릿 secure file은 권한이 느슨하면 읽지 않습니다.

## 7. 청소 전 판단 기준

- 재생성 가능한 것은 과감히 청소 가능
- 상태 원본과 실행 이력은 청소 전에 보존 여부를 먼저 판단

정리 기준표는 [CLEANUP.md](CLEANUP.md)를 함께 보면 됩니다.

## 8. 전체 목록이 필요할 때

아래 파일을 직접 보면 가장 정확합니다.

- `apps/omninode-middleware/src/AppConfig.cs`
- `apps/omninode-middleware/src/RuntimeSettings.cs`
- `apps/omninode-middleware/src/SecretLoader.cs`
